class AppSizes {
  AppSizes._();

  static const double screenPadding = 24;

  static const double dashboardCardPadding = 16;

  // Radius scale — small on chips/inputs, larger on cards/sheets,
  // so hierarchy reads through roundness, not just size.
  static const double radiusSm = 8;
  static const double radiusMd = 12;
  static const double radiusLg = 20;
  static const double radiusPill = 999;

  static const double cardRadius = radiusMd;

  static const double dashboardIconSize = 44;

  static const double largeIconSize = 64;

  static const double buttonHeight = 52;

  static const double buttonRadius = radiusMd;

  static const double textFieldRadius = radiusMd;

  // Rider-specific
  static const double statusChipHeight = 28;
  static const double avatarSize = 40;
  static const double orderCardImageSize = 56;
}
